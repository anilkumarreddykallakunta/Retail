import React from 'react';
import Dashboard from '../pages/Dashboard';

// Placeholder for future routing (React Router)
const AppRoutes = () => {
  return <Dashboard />;
};

export default AppRoutes;